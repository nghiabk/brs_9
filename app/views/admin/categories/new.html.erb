<% provide(:title, "new category") %>
<h1>New Category</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for([:admin, Category.new], html: {multipart: true}) do |builder| %>
      <%= render 'form_category', f: builder %>
    <% end %>
  </div>
</div>