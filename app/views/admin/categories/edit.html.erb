<% provide(:title, "edit category") %>
<h1>Edit category</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, @category] do |builder| %>
      <%= render 'form_category', f: builder %>
    <% end %>
  </div> 
</div>