<%= form_for (current_user.get_relationship?@user), html: {method: :delete},
                                                    remote: true do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>