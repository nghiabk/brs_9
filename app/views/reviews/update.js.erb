$('.book-star-rating').html("<%= j (render 'books/book_star_rating') %>");
$('.reviews').html("<%= j (render @book.reviews) %>");
$('.show-book-rate-star').html("<%= j (render_stars(@book.rating)) %>");
$('#book-rating').html("<%= @book.rating %>");